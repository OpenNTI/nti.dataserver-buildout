# Buildout mixin file for site configurations. Each site has a source section
# and an egg section; there are also a set of ALL sections that
# pull all known sites together for ease of testing and support
# of environments that want all site definitions

[server-versions]
sites = @0

[site-versions]
accenture-rwi = branch=master
alpha = branch=master
apple-rwi = branch=master
arcbest = branch=master
associationlearning = branch=master
bancfirst = branch=master
bellwetherenterprise = branch=master
careoklahoma = branch=master
chegg = branch=master
complyu = branch=master
dta = branch=master
electricalassociation = branch=master
ensync = branch=master
epiccharterschools = branch=master
esteem = branch=master
gammaphibeta = branch=master
hrpros = branch=master
ifsta = branch=master
iiaok = branch=master
lawokcu = branch=master
legalshield = branch=master
macsw = branch=master
mobar = branch=master
mygov = branch=master
nahb = branch=master
nrwa = branch=master
odapca = branch=master
onenet = branch=master
opsrc = branch=master
osde = branch=master
pairin = branch=master
pdca = branch=master
prmia = branch=master
proximity = branch=master
rdn1 = branch=master
rntpros = branch=master
rwi = branch=master
sallt = branch=master
smarttech = branch=master
strata = branch=master
thelanguagecompany = branch=master
theopp = branch=master
xyzuniversity = branch=master

[site-alpha-sources]
nti.app.sites.alpha = git git@github.com:NextThought/nti.app.sites.alpha.git ${site-versions:alpha}

[site-alpha-eggs]
eggs = nti.app.sites.alpha

[site-alpha-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = alpha.conf

global_host_name = alpha.nextthought.com beta.nextthought.com contentdev.nextthought.com alpha.dev alpha_child_11.nextthought.com alpha_child_12.nextthought.com nositecommunity-alpha.nextthought.com nositecommunity.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-alpha
extra_config =

[site-rwi-sources]
nti.app.sites.rwi = git git@github.com:NextThought/nti.app.sites.rwi.git ${site-versions:rwi}

[site-rwi-eggs]
eggs = nti.app.sites.rwi

[site-rwi-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = rwi.conf

global_host_name = rwi.nextthought.com demo-rwi.nextthought.com rwi-alpha.nextthought.com rwi-test.nextthought.com rwi.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-rwi
extra_config =

[site-accenture-rwi-sources]
nti.app.sites.accenture_rwi = git git@github.com:NextThought/nti.app.sites.accenture-rwi.git ${site-versions:accenture-rwi}

[site-accenture-rwi-eggs]
eggs = nti.app.sites.accenture_rwi

[site-accenture-rwi-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = accenture-rwi.conf

global_host_name = accenture-rwi.nextthought.com accenture-rwi-alpha.nextthought.com accenture-rwi-test.nextthought.com accenture-rwi.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-accenture-rwi
extra_config =

[site-ifsta-sources]
nti.app.sites.ifsta = git git@github.com:NextThought/nti.app.sites.ifsta.git ${site-versions:ifsta}

[site-ifsta-eggs]
eggs = nti.app.sites.ifsta

[site-ifsta-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = ifsta.conf

global_host_name = ifsta.nextthought.com ifsta-alpha.nextthought.com ifsta-test.nextthought.com ifsta.dev nextthought-fire1-alpha.nextthought.com nextthought-fire2-alpha.nextthought.com firehouse1.ifsta.online lasvegasnevada.ifsta.online coralsprings.ifsta.online kckfd.ifsta.online sandiego.ifsta.online delawareohio.ifsta.online kerncounty.ifsta.online cityofnorthport.ifsta.online cityofcowetaok.ifsta.online poncacityok.ifsta.online tngov.ifsta.online demo.ifsta.online
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-ifsta
extra_config =

[site-changemaker-sources]
nti.app.sites.changemaker = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.changemaker/trunk${server-versions:sites}

[site-changemaker-eggs]
eggs = nti.app.sites.changemaker

[site-changemaker-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = changemaker.conf

global_host_name = changemaker.nextthought.com changemaker-alpha.nextthought.com changemaker-test.nextthought.com changemaker.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-changemaker
extra_config =

[site-cceducare-sources]
nti.app.sites.cceducare = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.cceducare/trunk${server-versions:sites}

[site-cceducare-eggs]
eggs = nti.app.sites.cceducare

[site-cceducare-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = cceducare.conf

global_host_name = cceducare.nextthought.com cceducare-alpha.nextthought.com cceducare-test.nextthought.com cceducare.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-cceducare
extra_config =

[site-codesooner-sources]
nti.app.sites.codesooner = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.codesooner/trunk${server-versions:sites}

[site-codesooner-eggs]
eggs = nti.app.sites.codesooner

[site-codesooner-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = codesooner.conf

global_host_name = codesooner.ou.edu codesooner.nextthought.com codesooner-alpha.nextthought.com codesooner-test.nextthought.com codesooner.dev
landingpage_path = ${buildout:root-directory}/node_modules/@nti/content-landingpage-code-sooner/dist
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-codesooner
extra_config =

[site-complyu-sources]
nti.app.sites.complyu = git git@github.com:NextThought/nti.app.sites.complyu.git ${site-versions:complyu}

[site-complyu-eggs]
eggs = nti.app.sites.complyu

[site-complyu-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = complyu.conf

global_host_name = complyu.nextthought.com complyu-alpha.nextthought.com complyu-test.nextthought.com complyu.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-complyu
extra_config =

[site-darelearning-sources]
nti.app.sites.darelearning = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.darelearning/trunk${server-versions:sites}

[site-darelearning-eggs]
eggs = nti.app.sites.darelearning

[site-darelearning-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = darelearning.conf

global_host_name = darelearning.nextthought.com darelearning.org darelearning-alpha.nextthought.com darelearning-test.nextthought.com darelearning.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-darelearning
extra_config =

[site-engage-sources]
nti.app.sites.engage = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.engage/trunk${server-versions:sites}

[site-engage-eggs]
eggs = nti.app.sites.engage

[site-engage-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = engage.conf

global_host_name = engage.nextthought.com engage.sde.ok.gov engage-alpha.nextthought.com engage-test.nextthought.com engage.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-engage
extra_config =

[site-ensync-sources]
nti.app.sites.ensync = git git@github.com:NextThought/nti.app.sites.ensync.git ${site-versions:ensync}

[site-ensync-eggs]
eggs = nti.app.sites.ensync

[site-ensync-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = ensync.conf

global_host_name = ensync-corp.nextthought.com ensync-corp-alpha.nextthought.com ensync-corp-test.nextthought.com ensync-corp.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-ensync
extra_config =

[site-epiccharterschools-sources]
nti.app.sites.epiccharterschools = git git@github.com:NextThought/nti.app.sites.epiccharterschools.git ${site-versions:epiccharterschools}

[site-epiccharterschools-eggs]
eggs = nti.app.sites.epiccharterschools

[site-epiccharterschools-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = epiccharterschools.conf

global_host_name = epiccharterschools.nextthought.com epiccharterschools-alpha.nextthought.com epiccharterschools-test.nextthought.com epiccharterschools.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-epiccharterschools
extra_config =

[site-greatexpectations-sources]
nti.app.sites.greatexpectations = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.greatexpectations/trunk${server-versions:sites}

[site-greatexpectations-eggs]
eggs = nti.app.sites.greatexpectations

[site-greatexpectations-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = greatexpectations.conf

global_host_name = greatexpectations.nextthought.com greatexpectations-alpha.nextthought.com greatexpectations-test.nextthought.com greatexpectations.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-greatexpectations
extra_config =

[site-ona-sources]
nti.app.sites.ona = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.ona/trunk${server-versions:sites}

[site-ona-eggs]
eggs = nti.app.sites.ona

[site-ona-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = ona.conf

global_host_name = ona.nextthought.com ona-alpha.nextthought.com ona-test.nextthought.com ona.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-ona
extra_config =

[site-columbia-sources]
nti.app.sites.columbia = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.columbia/trunk${server-versions:sites}

[site-columbia-eggs]
eggs = nti.app.sites.columbia

[site-columbia-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = columbia.conf

global_host_name = columbia.nextthought.com columbia-alpha.nextthought.com columbia-test.nextthought.com columbia.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-columbia
extra_config =

[site-demo-sources]
nti.app.sites.demo = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.demo/trunk${server-versions:sites}

[site-demo-eggs]
eggs = nti.app.sites.demo

[site-demo-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = demo.conf

global_host_name = demo.nextthought.com demo-alpha.nextthought.com demo-test.nextthought.com demo.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-default
extra_config =

[site-edbooks-sources]
nti.app.sites.edbooks = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.edbooks/trunk${server-versions:sites}

[site-edbooks-eggs]
eggs = nti.app.sites.edbooks

[site-edbooks-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = edbooks.conf

global_host_name = edbooks.nextthought.com edbooks-alpha.nextthought.com edbooks-test.nextthought.com edbooks.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-edbooks
extra_config =

[site-utsa-sources]
nti.app.sites.utsa = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.utsa/trunk${server-versions:sites}

[site-utsa-eggs]
eggs = nti.app.sites.utsa

[site-law-sources]
nti.app.sites.law = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.law/trunk${server-versions:sites}

[site-law-eggs]
eggs = nti.app.sites.law

[site-litworld-sources]
nti.app.sites.litworld = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.litworld/trunk${server-versions:sites}

[site-litworld-eggs]
eggs = nti.app.sites.litworld

[site-litworld-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = litworld.conf

global_host_name = litworld.nextthought.com litworld-alpha.nextthought.com litworld-test.nextthought.com litworld.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-litworld
extra_config =

[site-mathcounts-sources]
nti.app.sites.mathcounts = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.mathcounts/trunk${server-versions:sites}

[site-mathcounts-eggs]
eggs = nti.app.sites.mathcounts

[site-mathcounts-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = mathcounts.conf

global_host_name = mathcounts.nextthought.com mathcounts-alpha.nextthought.com mathcounts-test.nextthought.com mathcounts.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-mathcounts
extra_config =

[site-personalleadorship-sources]
nti.app.sites.personalleadership = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.personalleadership/trunk${server-versions:sites}

[site-personalleadorship-eggs]
eggs = nti.app.sites.personalleadership

[site-prmia-sources]
nti.app.sites.prmia = git git@github.com:NextThought/nti.app.sites.prmia.git ${site-versions:prmia}

[site-prmia-eggs]
eggs = nti.app.sites.prmia

[site-prmia-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = prmia.conf

global_host_name = prmia.nextthought.com prmia-alpha.nextthought.com prmia-test.nextthought.com prmia.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-prmia
extra_config =

[site-proximity-sources]
nti.app.sites.proximity = git git@github.com:NextThought/nti.app.sites.proximity.git ${site-versions:proximity}

[site-proximity-eggs]
eggs = nti.app.sites.proximity

[site-proximity-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = proximity.conf

global_host_name = proximity.nextthought.com proximity-alpha.nextthought.com proximity-test.nextthought.com proximity.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-proximity
extra_config =

[site-rwanda-sources]
nti.app.sites.rwanda = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.rwanda/trunk${server-versions:sites}

[site-rwanda-eggs]
eggs = nti.app.sites.rwanda

[site-okchristian-sources]
nti.app.sites.okchristian = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.okchristian/trunk${server-versions:sites}

[site-okchristian-eggs]
eggs = nti.app.sites.okchristian

[site-oc-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = oc.conf

global_host_name = oc.nextthought.com oc-alpha.nextthought.com oc-test.nextthought.com oc.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-oc
extra_config =

[site-sallt-sources]
nti.app.sites.sallt = git git@github.com:NextThought/nti.app.sites.sallt.git ${site-versions:sallt}

[site-sallt-eggs]
eggs = nti.app.sites.sallt

[site-sallt-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = sallt.conf

global_host_name = sallt.nextthought.com sallt-alpha.nextthought.com sallt-test.nextthought.com sallt.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-sallt
extra_config =

[site-symmys-sources]
nti.app.sites.symmys = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.symmys/trunk${server-versions:sites}

[site-symmys-eggs]
eggs = nti.app.sites.symmys

[site-symmys-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = symmys.conf

global_host_name = lab.symmys.com symmys.nextthought.com symmys-alpha.nextthought.com symmys-test.nextthought.com symmys.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-symmys
extra_config =

[site-wiley-sources]
nti.app.sites.wiley = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.wiley/trunk${server-versions:sites}

[site-wiley-eggs]
eggs = nti.app.sites.wiley

[site-wiley-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
force-overwrite = true
files = wiley.conf

sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-wiley
landingpage_path = ${environment:global_content_directory}
global_host_name = wiley.nextthought.com wiley-alpha.nextthought.com wiley-test.nextthought.com wiley.dev
extra_config =

[site-morgan-wiley-sources]
nti.app.sites.morgan_wiley = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.morgan_wiley/trunk${server-versions:sites}

[site-morgan-wiley-eggs]
eggs = nti.app.sites.morgan_wiley

[site-alibra-sources]
nti.app.sites.alibra = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.alibra/trunk${server-versions:sites}

[site-alibra-eggs]
eggs = nti.app.sites.alibra

[site-augs-sources]
nti.app.sites.augs = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.augs/trunk${server-versions:sites}

[site-augs-eggs]
eggs = nti.app.sites.augs

[site-history-sources]
nti.app.sites.history = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.history/trunk${server-versions:sites}

[site-history-eggs]
eggs = nti.app.sites.history

[site-history-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = historychannel.conf

global_host_name = historychannel-alpha.nextthought.com
landingpage_path = ${buildout:root-directory}/node_modules/nti-content-landingpage-historychannel/dist
favicon_path = ${:landingpage_path}/metadata/favicon.ico

[site-ihs-sources]
nti.app.sites.ihs = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.ihs/trunk${server-versions:sites}

[site-ihs-eggs]
eggs = nti.app.sites.ihs

[site-performance-sources]
nti.app.sites.performance = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.performance/trunk${server-versions:sites}

[site-performance-eggs]
eggs = nti.app.sites.performance

[site-janux-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = janux.conf

global_host_name = janux.ou.edu platform.ou.edu ou-alpha.nextthought.com ou-test.nextthought.com janux.dev
landingpage_path = ${buildout:root-directory}/node_modules/nti-content-landingpage-janux/dist
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-janux
extra_config =

[site-okstate-sources]
nti.app.sites.okstate = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.okstate/trunk${server-versions:sites}

[site-okstate-eggs]
eggs = nti.app.sites.okstate

[site-okstate-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = okstate.conf

global_host_name = learnonline.okstate.edu okstate.nextthought.com okstate-test.nextthought.com okstate-alpha.nextthought.com okstate.dev
proxy_root = https://www.okstate.edu/
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-okstate
extra_config =

[site-connect-sources]
nti.app.sites.connect = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.connect/trunk${server-versions:sites}

[site-connect-eggs]
eggs = nti.app.sites.connect

[site-connect-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = connect.conf

global_host_name = connect.nextthought.com connect-alpha.nextthought.com connect-test.nextthought.com connect.dev
landingpage_path = ${buildout:root-directory}/node_modules/nti-content-landingpage-connect/dist
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-connect
extra_config =

[site-genlab-sources]
nti.app.sites.genlab = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.genlab/trunk${server-versions:sites}

[site-genlab-eggs]
eggs = nti.app.sites.genlab

[site-genlab-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = gen-lab.conf

global_host_name = gen-lab.nextthought.com gen-lab-alpha.nextthought.com gen-lab-test.nextthought.com gen-lab.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-gen-lab
extra_config =

[site-i2-sources]
nti.app.sites.i2 = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.i2/trunk${server-versions:sites}

[site-i2-eggs]
eggs = nti.app.sites.i2

[site-i2-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = i2.conf

global_host_name = i2.nextthought.com i2online.nextthought.com i2-alpha.nextthought.com i2-test.nextthought.com i2.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-i2
extra_config =

[site-iled-sources]
nti.app.sites.iled = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.iled/trunk${server-versions:sites}

[site-iled-eggs]
eggs = nti.app.sites.iled

[site-iled-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = iled.conf

global_host_name = iled.nextthought.com iled-alpha.nextthought.com iled-test.nextthought.com iled.dev
landingpage_path = ${buildout:root-directory}/node_modules/nti-content-landingpage-iled/dist
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-iled
extra_config =

[site-k20-sources]
nti.app.sites.k20 = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.k20/trunk${server-versions:sites}

[site-k20-eggs]
eggs = nti.app.sites.k20

[site-k20-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = k20.conf

global_host_name = k20.nextthought.com k20-alpha.nextthought.com k20-test.nextthought.com k20.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-default
extra_config =

[site-mcaops-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = mcaops.conf

global_host_name = mcaops-alpha.nextthought.com
landingpage_path = ${buildout:root-directory}/node_modules/nti-content-landingpage-mathcountsaopsou/dist
favicon_path = ${:landingpage_path}/metadata/favicon.ico

[site-movingup-sources]
nti.app.sites.movingup = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.movingup/trunk${server-versions:sites}

[site-movingup-eggs]
eggs = nti.app.sites.movingup

[site-movingup-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = movingup.conf

global_host_name = movingup.nextthought.com movingup-alpha.nextthought.com movingup-test.nextthought.com movingup.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-movingup
extra_config =

[site-osde-sources]
nti.app.sites.osde = git git@github.com:NextThought/nti.app.sites.osde.git ${site-versions:osde}

[site-osde-eggs]
eggs = nti.app.sites.osde

[site-osde-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = osde.conf

global_host_name = osdeconnect.ok.gov osde.nextthought.com osde-alpha.nextthought.com osde-test.nextthought.com osde.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-osde
extra_config =

[site-santafesouth-sources]
nti.app.sites.santafesouth = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.santafesouth/trunk${server-versions:sites}

[site-santafesouth-eggs]
eggs = nti.app.sites.santafesouth

[site-santafesouth-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
force-overwrite = true
files = santafesouth.conf

sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-santafesouth
landingpage_path = ${buildout:root-directory}/node_modules/nti-content-landingpage-santafesouth/dist
global_host_name = santafesouth.nextthought.com santafesouth-alpha.nextthought.com santafesouth-test.nextthought.com santafesouth.dev
extra_config =

[site-severeweather-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = severeweather.conf

global_host_name = gary-alpha.nextthought.com
landingpage_path = ${buildout:root-directory}/sources/nti.content.landingpage.severeweather/dist
favicon_path = ${:landingpage_path}/metadata/favicon.ico

[site-spurstartup-sources]
nti.app.sites.spurstartup = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.spurstartup/trunk${server-versions:sites}

[site-spurstartup-eggs]
eggs = nti.app.sites.spurstartup

[site-spurstartup-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
force-overwrite = true
files = spurstartup.conf

sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-spurstartup
landingpage_path = ${buildout:root-directory}/node_modules/nti-content-landingpage-spurstartup/dist
global_host_name = spurstartup.nextthought.com spurstartup-alpha.nextthought.com spurstartup-test.nextthought.com spurstartup.dev
extra_config =

[site-arlensa-sources]
nti.app.sites.arlensa = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.arlensa/trunk${server-versions:sites}

[site-arlensa-eggs]
eggs = nti.app.sites.arlensa

[site-arlensa-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = arlensa.conf

global_host_name = arlensa.nextthought.com arlensa-alpha.nextthought.com arlensa-test.nextthought.com arlensa.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-arlensa
extra_config =

[site-associationlearning-sources]
nti.app.sites.associationlearning = git git@github.com:NextThought/nti.app.sites.associationlearning.git ${site-versions:associationlearning}

[site-associationlearning-eggs]
eggs = nti.app.sites.associationlearning

[site-associationlearning-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = associationlearning.conf

global_host_name = associationsnext.org associationlearning.nextthought.com associationlearning-alpha.nextthought.com associationlearning-test.nextthought.com associationlearning.dev
landingpage_path = ${buildout:root-directory}/node_modules/@nti/content-landingpage-associationsnext/dist
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-associationlearning
extra_config =

[site-dpcedcenter-sources]
nti.app.sites.dpcedcenter = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.dpcedcenter/trunk${server-versions:sites}

[site-dpcedcenter-eggs]
eggs = nti.app.sites.dpcedcenter

[site-dpcedcenter-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = dpcedcenter.conf

global_host_name = dpcedcenter.nextthought.com dpcedcenter-alpha.nextthought.com dpcedcenter-test.nextthought.com dpcedcenter.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-dpcedcenter
extra_config =

[site-odapca-sources]
nti.app.sites.odapca = git git@github.com:NextThought/nti.app.sites.odapca.git ${site-versions:odapca}

[site-odapca-eggs]
eggs = nti.app.sites.odapca

[site-odapca-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = odapca.conf

global_host_name = odapca.nextthought.com odapca-alpha.nextthought.com odapca-test.nextthought.com odapca.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-odapca
extra_config =

[site-okbfaa-sources]
nti.app.sites.okbfaa = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.okbfaa/trunk${server-versions:sites}

[site-okbfaa-eggs]
eggs = nti.app.sites.okbfaa

[site-okbfaa-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = okbfaa.conf

global_host_name = okbfaa.nextthought.com okbfaa-alpha.nextthought.com okbfaa-test.nextthought.com okbfaa.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/nti-client-sites-okbfaa
extra_config =

[site-sevenedge-sources]
nti.app.sites.sevenedge = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.sevenedge/trunk${server-versions:sites}

[site-sevenedge-eggs]
eggs = nti.app.sites.sevenedge

[site-sevenedge-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = sevenedge.conf

global_host_name = sevenedge.nextthought.com sevenedge-alpha.nextthought.com sevenedge-test.nextthought.com sevenedge.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-sevenedge
extra_config =

[site-wecreatepeace-sources]
nti.app.sites.wecreatepeace = svn https://repos.nextthought.com/svn/nti-svn/nti.app.sites.wecreatepeace/trunk${server-versions:sites}

[site-wecreatepeace-eggs]
eggs = nti.app.sites.wecreatepeace

[site-wecreatepeace-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = wecreatepeace.conf

global_host_name = wecreatepeace.nextthought.com wecreatepeace-alpha.nextthought.com wecreatepeace-test.nextthought.com wecreatepeace.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-wecreatepeace
extra_config =

[site-nrwa-sources]
nti.app.sites.nrwa = git git@github.com:NextThought/nti.app.sites.nrwa.git ${site-versions:nrwa}

[site-nrwa-eggs]
eggs = nti.app.sites.nrwa

[site-nrwa-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = nrwa.conf

global_host_name = nrwa.nextthought.com nrwa-alpha.nextthought.com nrwa-test.nextthought.com nrwa.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-nrwa
extra_config =

[site-bellwetherenterprise-sources]
nti.app.sites.bellwetherenterprise = git git@github.com:NextThought/nti.app.sites.bellwetherenterprise.git ${site-versions:bellwetherenterprise}

[site-bellwetherenterprise-eggs]
eggs = nti.app.sites.bellwetherenterprise

[site-bellwetherenterprise-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = bellwetherenterprise.conf

global_host_name = bellwetherenterprise.nextthought.com bellwetherenterprise-alpha.nextthought.com bellwetherenterprise-test.nextthought.com bellwetherenterprise.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-bellwetherenterprise
extra_config =

[site-dta-sources]
nti.app.sites.dta = git git@github.com:NextThought/nti.app.sites.dta.git ${site-versions:dta}

[site-dta-eggs]
eggs = nti.app.sites.dta

[site-dta-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = dta.conf

global_host_name = dta.nextthought.com dta-alpha.nextthought.com dta-test.nextthought.com dta.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-dta
extra_config =

[site-esteem-sources]
nti.app.sites.esteem = git git@github.com:NextThought/nti.app.sites.esteem.git ${site-versions:esteem}

[site-esteem-eggs]
eggs = nti.app.sites.esteem

[site-esteem-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = esteem.conf

global_host_name = esteem.nextthought.com esteem-alpha.nextthought.com esteem-test.nextthought.com esteem.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-esteem
extra_config =

[site-chegg-sources]
nti.app.sites.chegg = git git@github.com:NextThought/nti.app.sites.chegg.git ${site-versions:chegg}

[site-chegg-eggs]
eggs = nti.app.sites.chegg

[site-chegg-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = chegg.conf

global_host_name = chegg.nextthought.com chegg-alpha.nextthought.com chegg-test.nextthought.com chegg.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-chegg
extra_config =

[site-nahb-sources]
nti.app.sites.nahb = git git@github.com:NextThought/nti.app.sites.nahb.git ${site-versions:nahb}

[site-nahb-eggs]
eggs = nti.app.sites.nahb

[site-nahb-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = nahb.conf

global_host_name = nahb.nextthought.com nahb-alpha.nextthought.com nahb-test.nextthought.com nahb.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-nahb
extra_config =

[site-rntpros-sources]
nti.app.sites.rntpros = git git@github.com:NextThought/nti.app.sites.rntpros.git ${site-versions:rntpros}

[site-rntpros-eggs]
eggs = nti.app.sites.rntpros

[site-rntpros-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = rntpros.conf

global_host_name = rntpros.nextthought.com rntpros-alpha.nextthought.com rntpros-test.nextthought.com rntpros.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-rntpros
extra_config =

[site-iiaok-sources]
nti.app.sites.iiaok = git git@github.com:NextThought/nti.app.sites.iiaok.git ${site-versions:iiaok}

[site-iiaok-eggs]
eggs = nti.app.sites.iiaok

[site-iiaok-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = iiaok.conf

global_host_name = iiaok.nextthought.com iiaok-alpha.nextthought.com iiaok-test.nextthought.com iiaok.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-iiaok
extra_config =

[site-proximity-sources]
nti.app.sites.proximity = git git@github.com:NextThought/nti.app.sites.proximity.git ${site-versions:proximity}

[site-proximity-eggs]
eggs = nti.app.sites.proximity

[site-proximity-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = proximity.conf

global_host_name = proximityschool.nextthought.com proximity.nextthought.com proximity-alpha.nextthought.com proximity-test.nextthought.com proximity.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-proximity
extra_config =

[site-apple-rwi-sources]
nti.app.sites.apple_rwi = git git@github.com:NextThought/nti.app.sites.apple-rwi.git ${site-versions:apple-rwi}

[site-apple-rwi-eggs]
eggs = nti.app.sites.apple_rwi

[site-apple-rwi-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = apple-rwi.conf

global_host_name = apple.nextthought.com apple-rwi.nextthought.com apple-rwi-alpha.nextthought.com apple-rwi-test.nextthought.com apple-rwi.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-apple-rwi
extra_config =

[site-pdca-sources]
nti.app.sites.pdca = git git@github.com:NextThought/nti.app.sites.pdca.git ${site-versions:pdca}

[site-pdca-eggs]
eggs = nti.app.sites.pdca

[site-pdca-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = pdca.conf

global_host_name = pdca.nextthought.com pdca-alpha.nextthought.com pdca-test.nextthought.com pdca.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-pdca
extra_config =

[site-opsrc-sources]
nti.app.sites.opsrc = git git@github.com:NextThought/nti.app.sites.opsrc.git ${site-versions:opsrc}

[site-opsrc-eggs]
eggs = nti.app.sites.opsrc

[site-opsrc-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = opsrc.conf

global_host_name = opsrc.online *.opsrc.online opsrc.nextthought.com opsrc-alpha.nextthought.com opsrc-alpha-child-11.nextthought.com opsrc-alpha-child-12.nextthought.com opsrc-test.nextthought.com opsrc.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-opsrc
extra_config =

[site-macsw-sources]
nti.app.sites.macsw = git git@github.com:NextThought/nti.app.sites.macsw.git ${site-versions:macsw}

[site-macsw-eggs]
eggs = nti.app.sites.macsw

[site-macsw-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = macsw.conf

global_host_name = macsw.nextthought.com macsw-alpha.nextthought.com macsw-test.nextthought.com macsw.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-macsw
extra_config =

[site-rdn1-sources]
nti.app.sites.rdn1 = git git@github.com:NextThought/nti.app.sites.rdn1.git ${site-versions:rdn1}

[site-rdn1-eggs]
eggs = nti.app.sites.rdn1

[site-rdn1-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = rdn1.conf

global_host_name = rdn1.nextthought.com rdn1-alpha.nextthought.com rdn1-test.nextthought.com rdn1.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-rdn1
extra_config =

[site-careoklahoma-sources]
nti.app.sites.careoklahoma = git git@github.com:NextThought/nti.app.sites.careoklahoma.git ${site-versions:careoklahoma}

[site-careoklahoma-eggs]
eggs = nti.app.sites.careoklahoma

[site-careoklahoma-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = careoklahoma.conf

global_host_name = careoklahoma.nextthought.com careoklahoma-alpha.nextthought.com careoklahoma-test.nextthought.com careoklahoma.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-careoklahoma
extra_config =

[site-smarttech-sources]
nti.app.sites.smarttech = git git@github.com:NextThought/nti.app.sites.smarttech.git ${site-versions:smarttech}

[site-smarttech-eggs]
eggs = nti.app.sites.smarttech

[site-smarttech-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = smarttech.conf

global_host_name = smarttech.nextthought.com smarttech-alpha.nextthought.com smarttech-test.nextthought.com smarttech.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-smarttech
extra_config =

[site-xyzuniversity-sources]
nti.app.sites.xyzuniversity = git git@github.com:NextThought/nti.app.sites.xyzuniversity.git ${site-versions:xyzuniversity}

[site-xyzuniversity-eggs]
eggs = nti.app.sites.xyzuniversity

[site-xyzuniversity-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = xyzuniversity.conf

global_host_name = xyzuniversity.nextthought.com xyzuniversity-alpha.nextthought.com xyzuniversity-test.nextthought.com xyzuniversity.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-xyzuniversity
extra_config =

[site-bancfirst-sources]
nti.app.sites.bancfirst = git git@github.com:NextThought/nti.app.sites.bancfirst.git ${site-versions:bancfirst}

[site-bancfirst-eggs]
eggs = nti.app.sites.bancfirst

[site-bancfirst-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = bancfirst.conf

global_host_name = bancfirst.nextthought.com bancfirst-alpha.nextthought.com bancfirst-test.nextthought.com bancfirst.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-bancfirst
extra_config =

[site-thelanguagecompany-sources]
nti.app.sites.thelanguagecompany = git git@github.com:NextThought/nti.app.sites.thelanguagecompany.git ${site-versions:thelanguagecompany}

[site-thelanguagecompany-eggs]
eggs = nti.app.sites.thelanguagecompany

[site-thelanguagecompany-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = thelanguagecompany.conf

global_host_name = thelanguagecompany.nextthought.com thelanguagecompany-alpha.nextthought.com thelanguagecompany-test.nextthought.com thelanguagecompany.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-thelanguagecompany
extra_config =

[site-lawokcu-sources]
nti.app.sites.lawokcu = git git@github.com:NextThought/nti.app.sites.lawokcu.git ${site-versions:lawokcu}

[site-lawokcu-eggs]
eggs = nti.app.sites.lawokcu

[site-lawokcu-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = lawokcu.conf

global_host_name = lawokcu.nextthought.com lawokcu-alpha.nextthought.com lawokcu-test.nextthought.com lawokcu.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-lawokcu
extra_config =

[site-onenet-sources]
nti.app.sites.onenet = git git@github.com:NextThought/nti.app.sites.onenet.git ${site-versions:onenet}

[site-onenet-eggs]
eggs = nti.app.sites.onenet

[site-onenet-nginx]
recipe = z3c.recipe.filetemplate
source-directory = templates
dest-directory = ${deployment:root-directory}
# the main conf file is already installed
# as part of building nginx, we must overwrite it
force-overwrite = true
files = onenet.conf

global_host_name = onenet.nextthought.com onenet-alpha.nextthought.com onenet-test.nextthought.com onenet.dev
landingpage_path = ${environment:global_content_directory}
sitepackage_location = ${buildout:root-directory}/node_modules/@nti/client-sites-onenet
extra_config =

[site-gammaphibeta-sources]
nti.app.sites.gammaphibeta = git git@github.com:NextThought/nti.app.sites.gammaphibeta.git ${site-versions:gammaphibeta}

[site-gammaphibeta-eggs]
eggs = nti.app.sites.gammaphibeta

[site-mygov-sources]
nti.app.sites.mygov = git git@github.com:NextThought/nti.app.sites.mygov.git ${site-versions:mygov}

[site-mygov-eggs]
eggs = nti.app.sites.mygov

[site-strata-sources]
nti.app.sites.strata = git git@github.com:NextThought/nti.app.sites.strata.git ${site-versions:strata}

[site-strata-eggs]
eggs = nti.app.sites.strata

[site-pairin-sources]
nti.app.sites.pairin = git git@github.com:NextThought/nti.app.sites.pairin.git ${site-versions:pairin}

[site-pairin-eggs]
eggs = nti.app.sites.pairin

[site-arcbest-sources]
nti.app.sites.arcbest = git git@github.com:NextThought/nti.app.sites.arcbest.git ${site-versions:arcbest}

[site-arcbest-eggs]
eggs = nti.app.sites.arcbest

[site-legalshield-sources]
nti.app.sites.legalshield = git git@github.com:NextThought/nti.app.sites.legalshield.git ${site-versions:legalshield}

[site-legalshield-eggs]
eggs = nti.app.sites.legalshield

[site-electricalassociation-sources]
nti.app.sites.electricalassociation = git git@github.com:NextThought/nti.app.sites.electricalassociation.git ${site-versions:electricalassociation}

[site-electricalassociation-eggs]
eggs = nti.app.sites.electricalassociation


[site-theopp-sources]
nti.app.sites.theopp = git git@github.com:NextThought/nti.app.sites.theopp.git ${site-versions:theopp}

[site-theopp-eggs]
eggs = nti.app.sites.theopp


[site-mobar-sources]
nti.app.sites.mobar = git git@github.com:NextThought/nti.app.sites.mobar.git ${site-versions:mobar}

[site-mobar-eggs]
eggs = nti.app.sites.mobar


[site-hrpros-sources]
nti.app.sites.hrpros = git git@github.com:NextThought/nti.app.sites.hrpros.git ${site-versions:hrpros}

[site-hrpros-eggs]
eggs = nti.app.sites.hrpros


[site-ALL-nginx]
nginx =	site-alpha-nginx
		site-ifsta-nginx
		site-changemaker-nginx
		site-columbia-nginx
		site-connect-nginx
		site-demo-nginx
		site-edbooks-nginx
		site-genlab-nginx
		site-greatexpectations-nginx
		site-history-nginx
		site-i2-nginx
		site-iiaok-nginx
		site-iled-nginx
		site-janux-nginx
		site-k20-nginx
		site-rwi-nginx
		site-litworld-nginx
		site-mathcounts-nginx
		site-mcaops-nginx
		site-movingup-nginx
		site-nrwa-nginx
		site-oc-nginx
		site-okstate-nginx
		site-ona-nginx
		site-osde-nginx
		site-prmia-nginx
		site-proximity-nginx
		site-santafesouth-nginx
		site-severeweather-nginx
		site-spurstartup-nginx
		site-symmys-nginx
		site-arlensa-nginx
		site-codesooner-nginx
		site-wiley-nginx
		site-cceducare-nginx
		site-okbfaa-nginx
		site-odapca-nginx
		site-dpcedcenter-nginx
		site-accenture-rwi-nginx
		site-ensync-nginx
		site-complyu-nginx
		site-sallt-nginx
		site-epiccharterschools-nginx
		site-associationlearning-nginx
		site-darelearning-nginx
		site-wecreatepeace-nginx
		site-engage-nginx
		site-sevenedge-nginx
		site-bellwetherenterprise-nginx
		site-dta-nginx
		site-esteem-nginx
		site-chegg-nginx
		site-nahb-nginx
		site-rdn1-nginx
		site-rntpros-nginx
		site-proximity-nginx
		site-apple-rwi-nginx
		site-pdca-nginx
		site-opsrc-nginx
		site-macsw-nginx
		site-careoklahoma-nginx
		site-smarttech-nginx
		site-xyzuniversity-nginx
		site-bancfirst-nginx
		site-thelanguagecompany-nginx
		site-lawokcu-nginx
		site-onenet-nginx

[site-ALL-sources]
<= 	site-alpha-sources
	site-ona-sources
	site-ifsta-sources
	site-changemaker-sources
	site-greatexpectations-sources
	site-columbia-sources
	site-demo-sources
	site-edbooks-sources
	site-utsa-sources
	site-law-sources
	site-litworld-sources
	site-mathcounts-sources
	site-okchristian-sources
	site-personalleadorship-sources
	site-prmia-sources
	site-proximity-sources
	site-rwanda-sources
	site-symmys-sources
	site-alibra-sources
	site-wiley-sources
	site-augs-sources
	site-history-sources
	site-genlab-sources
	site-ihs-sources
	site-iiaok-sources
	site-i2-sources
	site-k20-sources
	site-iled-sources
	site-okstate-sources
	site-connect-sources
	site-columbia-sources
	site-morgan-wiley-sources
	site-performance-sources
	site-spurstartup-sources
	site-mathcounts-sources
	site-santafesouth-sources
	site-movingup-sources
	site-codesooner-sources
	site-osde-sources
	site-rwi-sources
	site-okbfaa-sources
	site-odapca-sources
	site-arlensa-sources
	site-cceducare-sources
	site-dpcedcenter-sources
	site-accenture-rwi-sources
	site-ensync-sources
	site-complyu-sources
	site-sallt-sources
	site-epiccharterschools-sources
	site-associationlearning-sources
	site-darelearning-sources
	site-wecreatepeace-sources
	site-engage-sources
	site-sevenedge-sources
	site-nrwa-sources
	site-bellwetherenterprise-sources
	site-dta-sources
	site-esteem-sources
	site-chegg-sources
	site-nahb-sources
	site-rdn1-sources
	site-rntpros-sources
	site-proximity-sources
	site-apple-rwi-sources
	site-pdca-sources
	site-opsrc-sources
	site-macsw-sources
	site-careoklahoma-sources
	site-smarttech-sources
	site-xyzuniversity-sources
	site-bancfirst-sources
	site-thelanguagecompany-sources
	site-lawokcu-sources
	site-onenet-sources
	site-gammaphibeta-sources
	site-mygov-sources
	site-strata-sources
	site-pairin-sources
	site-arcbest-sources
	site-legalshield-sources
	site-electricalassociation-sources
	site-theopp-sources
	site-mobar-sources
	site-hrpros-sources

[site-ALL-eggs]
eggs =
	 nti.app.sites.alpha
	 nti.app.sites.rwi
	 nti.app.sites.ona
	 nti.app.sites.ifsta
	 nti.app.sites.changemaker
	 nti.app.sites.columbia
	 nti.app.sites.demo
	 nti.app.sites.edbooks
	 nti.app.sites.utsa
	 nti.app.sites.law
	 nti.app.sites.litworld
	 nti.app.sites.mathcounts
	 nti.app.sites.okchristian
	 nti.app.sites.greatexpectations
	 nti.app.sites.personalleadership
	 nti.app.sites.prmia
	 nti.app.sites.proximity
	 nti.app.sites.rwanda
	 nti.app.sites.symmys
	 nti.app.sites.wiley
	 nti.app.sites.alibra
	 nti.app.sites.augs
	 nti.app.sites.genlab
	 nti.app.sites.ihs
	 nti.app.sites.iiaok
	 nti.app.sites.i2
	 nti.app.sites.k20
	 nti.app.sites.iled
	 nti.app.sites.arlensa
	 nti.app.sites.movingup
	 nti.app.sites.okstate
	 nti.app.sites.osde
	 nti.app.sites.history
	 nti.app.sites.connect
	 nti.app.sites.columbia
	 nti.app.sites.spurstartup
	 nti.app.sites.santafesouth
	 nti.app.sites.morgan_wiley
	 nti.app.sites.performance
	 nti.app.sites.codesooner
	 nti.app.sites.okbfaa
	 nti.app.sites.odapca
	 nti.app.sites.cceducare
	 nti.app.sites.dpcedcenter
	 nti.app.sites.accenture_rwi
	 nti.app.sites.ensync
	 nti.app.sites.complyu
	 nti.app.sites.sallt
	 nti.app.sites.epiccharterschools
	 nti.app.sites.associationlearning
	 nti.app.sites.darelearning
	 nti.app.sites.wecreatepeace
	 nti.app.sites.engage
	 nti.app.sites.sevenedge
	 nti.app.sites.nrwa
	 nti.app.sites.bellwetherenterprise
	 nti.app.sites.dta
	 nti.app.sites.esteem
	 nti.app.sites.chegg
	 nti.app.sites.nahb
	 nti.app.sites.rdn1
	 nti.app.sites.rntpros
	 nti.app.sites.proximity
	 nti.app.sites.apple_rwi
	 nti.app.sites.pdca
	 nti.app.sites.opsrc
	 nti.app.sites.macsw
	 nti.app.sites.careoklahoma
	 nti.app.sites.smarttech
	 nti.app.sites.xyzuniversity
	 nti.app.sites.bancfirst
	 nti.app.sites.thelanguagecompany
	 nti.app.sites.lawokcu
	 nti.app.sites.onenet
	 nti.app.sites.gammaphibeta
	 nti.app.sites.mygov
	 nti.app.sites.strata
	 nti.app.sites.pairin
	 nti.app.sites.arcbest
	 nti.app.sites.legalshield
	 nti.app.sites.electricalassociation
	 nti.app.sites.theopp
	 nti.app.sites.mobar
	 nti.app.sites.hrpros
