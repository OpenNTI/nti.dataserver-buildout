[buildout]
parts =
		notable-digest-alpha-cron
		update-webinars-alpha-cron

extensions = mr.developer
mr.developer-threads = 35
sources-dir = sources
always-checkout = true
auto-checkout = *

[sources]
nti.tools.scripts = git git@github.com:NextThought/nti.tools.scripts.git branch=master egg=false

[eggs]
recipe = zc.recipe.egg
eggs =
	 ansible
interpreter = python
dependent-scripts = true

[notable-digest-alpha-cron]
recipe = z3c.recipe.usercrontab
script_dir = ${buildout:directory}/${buildout:sources-dir}/nti.tools.scripts
times = 15 13 * * *
command = ${:script_dir}/bin/ops/test_notable_digest alpha.nextthought.com

[update-webinars-alpha-cron]
recipe = z3c.recipe.usercrontab
script_dir = ${buildout:directory}/${buildout:sources-dir}/nti.tools.scripts
times = 10 * * * *
command = ${:script_dir}/bin/content/update_webinars alpha.nextthought.com
