#!/bin/bash

# Put sencha on the path
export PATH=${senchacmd:location|shell-path}:$PATH

cd ${environment:webapp_path|shell-path}
# First, compass compile
../../parts/senchacmd/sencha compass compile

# Run the minify. Sadly it assumes that it is run
# from the directory containing the source so we must go there
cd ${environment:webapp_path/src/main|shell-path}
${python|shell-path} ${environment:webapp_path|shell-path}/src/main/app-minify.py $*
