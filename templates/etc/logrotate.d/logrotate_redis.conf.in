${deployment:log-directory}/redis-std*---supervisor-*.log {
    daily
    rotate 7
    dateext
    nocompress
    notifempty
    missingok
    sharedscripts
    postrotate
        kill -USR2 `cat ${deployment:var-directory}/supervisord.pid`
    endscript
}
