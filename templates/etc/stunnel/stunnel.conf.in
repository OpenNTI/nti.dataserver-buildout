cert = ${stunnel_cert_file}
fips = no
debug = debug
pid = ${deployment:run-directory/stunnel.pid}
foreground = ${stunnel_foreground}
[https]
accept = ${stunnel_https_port}
#verify = 0
#connect = 80
connect = ${stunnel_socket}
protocol = proxy
sslVersion = all
TIMEOUTclose = 0
