[buildout]

parts =
		stanford-postagger-installer
		stanford-postagger

[stanford-postagger-installer]
recipe = hexagonit.recipe.download
url = https://downloads.nextthought.com/deps/stanford-postagger.zip
destination=${buildout:directory}/parts/stanford-postagger
strip-top-level-dir = true

[stanford-postagger]
recipe = z3c.recipe.filetemplate
source-directory = templates
files = stanford-postagger
java-cmd = java
java-opts = -Xmx300m
libraries = ${stanford-postagger-installer:destination}/lib
models = ${stanford-postagger-installer:destination}/models
