[buildout]
extends =
	openssl.cfg

[cryptography_env]
ARCHFLAGS = -arch x86_64
CFLAGS = -I${openssl:prefix}/include
LDFLAGS = -L${openssl:prefix}/lib
 
[cryptography]
recipe = zc.recipe.egg:custom
environment = cryptography_env
force = true
egg = cryptography

